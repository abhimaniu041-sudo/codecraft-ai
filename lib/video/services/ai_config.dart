// ── AI Configuration ──────────────────────────────────────
// Add your API keys here
class AIConfig {
  // Primary AI - Groq (already in your app)
  static const String groqApiKey = 'YOUR_GROQ_API_KEY_HERE';

  // Backup AIs (add keys to enable)
  static const String geminiApiKey = '';       // Add Gemini key
  static const String openRouterApiKey = '';   // Add OpenRouter key
  static const String huggingFaceApiKey = '';  // Add HuggingFace key
}
